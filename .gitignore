.cargo/
*.pdb
**/*.rs.bk
debug/
target/
vendor/
vendor.tar
debian/*
!debian/changelog
!debian/control
!debian/copyright
!debian/install
!debian/rules
!debian/source

# loom workspace state
.work/
.work

# loom worktrees
.worktrees/
.worktrees

# Claude Code per-machine settings
.claude/settings.local.json

# stray marker
.codex

# local-only helper scripts (but ship the user-facing udev installer)
scripts/*
!scripts/install-liquidctl-udev.sh
